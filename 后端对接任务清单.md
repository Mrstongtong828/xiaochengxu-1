# 后端对接任务清单

## 数据库设计（如果不用 uniCloud）

### 表1：users（用户表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int/bigint | 主键，自增 |
| phone | varchar(20) | 手机号 |
| nickname | varchar(100) | 昵称 |
| avatar | varchar(255) | 头像 URL |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

### 表2：tokens（令牌表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int/bigint | 主键 |
| user_id | int/bigint | 用户 ID |
| token | varchar(255) | 令牌 |
| create_time | datetime | 创建时间 |
| expire_time | datetime | 过期时间 |

### 表3：sms_codes（短信验证码表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int/bigint | 主键 |
| phone | varchar(20) | 手机号 |
| code | varchar(10) | 验证码 |
| create_time | datetime | 创建时间 |
| expire_time | datetime | 过期时间 |
| used | tinyint | 是否已使用（0/1） |

### 表4：repair_orders（维修订单表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int/bigint | 主键 |
| order_no | varchar(50) | 工单号 |
| user_id | int/bigint | 用户 ID |
| receiver_name | varchar(100) | 收件人姓名 |
| receiver_phone | varchar(20) | 收件人电话 |
| receiver_address | varchar(255) | 收件地址 |
| receiver_unit | varchar(255) | 单位名称 |
| logistics_company | varchar(50) | 客户寄出物流公司 |
| tracking_no | varchar(64) | 客户寄出快递单号 |
| products | text/json | 维修产品列表（JSON 格式） |
| status | varchar(20) | 状态（pending/fixing/completed/shipped） |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

### 表5：package_receipts（包裹签收/录入表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int/bigint | 主键 |
| tracking_no | varchar(64) | 快递单号，建议唯一索引 |
| company | varchar(50) | 物流公司 |
| phone_last4 | varchar(4) | 客户手机号后四位，可为空 |
| order_no | varchar(50) | 关联工单号，可为空 |
| status | varchar(30) | 状态（waiting/received/checking/processing/done） |
| timeline | text/json | 包裹时间线 |
| signed_at | datetime | 签收时间 |
| created_at | datetime | 录入时间 |
| updated_at | datetime | 更新时间 |

---

## API 接口

### 1. 发送验证码

- **URL**：`POST /api/auth/sendCode`
- **参数**：`{ phone: "13800138000" }`
- **返回**：

```json
{
  "code": 0,
  "message": "验证码发送成功"
}
```

### 2. 登录

- **URL**：`POST /api/auth/login`
- **参数**：`{ phone: "13800138000", code: "123456" }`
- **返回**：

```json
{
  "code": 0,
  "message": "登录成功",
  "data": {
    "token": "xxx",
    "userInfo": {
      "id": 1,
      "phone": "13800138000",
      "nickname": "用户8000"
    }
  }
}
```

### 3. 提交维修订单

- **URL**：`POST /api/repair/submit`
- **Header**：`Authorization: Bearer {token}`
- **参数**：

```json
{
  "receiverName": "李医生",
  "receiverPhone": "13800138000",
  "receiverAddress": "桂林口腔门诊",
  "receiverUnit": "桂林口腔门诊有限公司",
  "logisticsCompany": "顺丰",
  "trackingNo": "SF1234567890",
  "products": [
    {
      "name": "高速手机",
      "model": "KAVO 9000",
      "serial": "SN123456",
      "buyDate": "2023-05-01",
      "voucherImages": ["url1", "url2"],
      "faultDesc": "设备无法启动",
      "images": ["url3"],
      "videos": []
    }
  ]
}
```

- **返回**：

```json
{
  "code": 0,
  "message": "提交成功",
  "data": {
    "id": 1,
    "orderNo": "WX1234567890"
  }
}
```

### 4. 获取维修订单列表

- **URL**：`GET /api/repair/list`
- **Header**：`Authorization: Bearer {token}`
- **参数**：`{ status?: "pending", page?: 1, pageSize?: 10 }`
- **返回**：

```json
{
  "code": 0,
  "data": {
    "list": [
      {
        "id": 1,
        "orderNo": "WX1234567890",
        "status": "pending",
        "createTime": 1234567890,
        "products": []
      }
    ],
    "total": 10,
    "page": 1,
    "pageSize": 10
  }
}
```

### 5. 查询包裹状态

- **URL**：`GET /api/package/query`
- **参数**：`{ trackingNo: "SF1234567890", phoneLast4?: "8000" }`
- **说明**：
  - `trackingNo` 必填
  - `phoneLast4` 选填，用于身份核验
  - 后台签收并录入快递单号后，前端才能查到记录
- **返回**：

```json
{
  "code": 0,
  "data": {
    "trackingNo": "SF1234567890",
    "company": "顺丰",
    "status": "已签收",
    "tone": "success",
    "orderId": "WX1234567890",
    "reached": 2,
    "timeline": [
      {
        "title": "包裹已签收",
        "time": "2026-05-17 10:20",
        "desc": "前台完成签收并录入系统"
      },
      {
        "title": "等待工程师确认",
        "time": "2026-05-17 11:10",
        "desc": "已进入待处理队列"
      }
    ]
  }
}
```

---

## 后端开发任务

### 阶段一：基础功能（优先级：高）

- [ ] 数据库建表（users/tokens/sms_codes/repair_orders）
- [ ] 发送验证码接口（`/api/auth/sendCode`）
- [ ] 登录接口（`/api/auth/login`）
- [ ] 提交维修订单接口（`/api/repair/submit`）

### 阶段二：完整功能（优先级：中）

- [ ] 获取维修订单列表接口（`/api/repair/list`）
- [ ] 获取维修订单详情接口
- [ ] 取消维修订单接口
- [ ] Token 验证中间件
- [ ] 包裹签收录入表与后台录入流程
- [ ] 包裹查询接口（`/api/package/query`）

### 阶段三：管理后台（优先级：低）

- [ ] 后台订单列表
- [ ] 后台订单状态更新
- [ ] 后台订单详情
- [ ] 后台用户管理
- [ ] 后台包裹签收录入与状态维护

---

## 费用与接入提醒

以下能力通常可能带来额外成本，建议在项目立项时单独评估：

1. **短信验证码**：按短信条数计费。
2. **图片/视频上传**：对象存储与 CDN 可能计费。
3. **第三方物流轨迹查询**：如果不仅查内部签收，还想查实时物流轨迹，通常需要第三方物流接口服务。
4. **微信正式发布**：如果要正式对外使用，需按微信公众平台当期认证规则执行。

如果只是做开发版/体验版给客户预览，一般不额外收费。

---

## 注意事项

1. **Token 验证**：所有需要登录的接口都要校验 `Authorization`。
2. **统一返回格式**：统一使用 `code / message / data`。
3. **错误处理**：返回明确错误信息，方便前端提示。
4. **订单号生成**：建议使用 `WX + 时间戳/序列`。
5. **时间格式**：建议统一为毫秒时间戳或标准时间字符串。
6. **包裹查询边界**：当前前端查的是“是否收到包裹以及内部处理状态”，不是强依赖第三方物流全链路轨迹。
