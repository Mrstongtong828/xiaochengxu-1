# 后端对接任务清单

## 数据库设计（如果不用uniCloud）

### 表1: users（用户表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | int/bigint | 主键，自增 |
| phone | varchar(20) | 手机号 |
| nickname | varchar(100) | 昵称 |
| avatar | varchar(255) | 头像URL |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

### 表2: tokens（令牌表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | int/bigint | 主键 |
| user_id | int/bigint | 用户ID |
| token | varchar(255) | 令牌 |
| create_time | datetime | 创建时间 |
| expire_time | datetime | 过期时间 |

### 表3: sms_codes（短信验证码表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | int/bigint | 主键 |
| phone | varchar(20) | 手机号 |
| code | varchar(10) | 验证码 |
| create_time | datetime | 创建时间 |
| expire_time | datetime | 过期时间 |
| used | tinyint | 是否已使用（0/1） |

### 表4: repair_orders（维修订单表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | int/bigint | 主键 |
| order_no | varchar(50) | 订单号 |
| user_id | int/bigint | 用户ID |
| receiver_name | varchar(100) | 收件人姓名 |
| receiver_phone | varchar(20) | 收件人电话 |
| receiver_address | varchar(255) | 收件地址 |
| products | text/json | 维修产品列表（JSON格式） |
| status | varchar(20) | 状态（pending/fixing/completed/shipped） |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

---

## API接口

### 1. 发送验证码
- **URL**: `POST /api/auth/sendCode`
- **参数**: `{ phone: "13800138000" }`
- **返回**:
  ```json
  {
    "code": 0,
    "message": "验证码发送成功"
  }
  ```

### 2. 登录
- **URL**: `POST /api/auth/login`
- **参数**: `{ phone: "13800138000", code: "123456" }`
- **返回**:
  ```json
  {
    "code": 0,
    "message": "登录成功",
    "data": {
      "token": "xxx",
      "userInfo": {
        "id": 1,
        "phone": "13800138000",
        "nickname": "用户8000"
      }
    }
  }
  ```

### 3. 提交维修订单
- **URL**: `POST /api/repair/submit`
- **Header**: `Authorization: Bearer {token}`
- **参数**:
  ```json
  {
    "receiverName": "李医生",
    "receiverPhone": "13800138000",
    "receiverAddress": "桂林口腔门诊",
    "products": [
      {
        "name": "高速手机",
        "model": "KAVO 9000",
        "serial": "SN123456",
        "buyDate": "2023-05-01",
        "voucherImages": ["url1", "url2"],
        "faultDesc": "设备无法启动",
        "images": ["url3"],
        "videos": []
      }
    ]
  }
  ```
- **返回**:
  ```json
  {
    "code": 0,
    "message": "提交成功",
    "data": {
      "id": 1,
      "orderNo": "WX1234567890"
    }
  }
  ```

### 4. 获取维修订单列表
- **URL**: `GET /api/repair/list`
- **Header**: `Authorization: Bearer {token}`
- **参数**: `{ status?: "pending", page?: 1, pageSize?: 10 }`
- **返回**:
  ```json
  {
    "code": 0,
    "data": {
      "list": [
        {
          "id": 1,
          "orderNo": "WX1234567890",
          "status": "pending",
          "createTime": 1234567890,
          "products": [...]
        }
      ],
      "total": 10,
      "page": 1,
      "pageSize": 10
    }
  }
  ```

---

## 后端开发任务

### 阶段一：基础功能（优先级：高）
- [ ] 数据库建表（users/tokens/sms_codes/repair_orders）
- [ ] 发送验证码接口（/api/auth/sendCode）
- [ ] 登录接口（/api/auth/login）
- [ ] 提交维修订单接口（/api/repair/submit）

### 阶段二：完整功能（优先级：中）
- [ ] 获取维修订单列表接口（/api/repair/list）
- [ ] 获取维修订单详情接口
- [ ] 取消维修订单接口
- [ ] Token验证中间件

### 阶段三：管理后台（优先级：低）
- [ ] 后台订单列表
- [ ] 后台订单状态更新
- [ ] 后台订单详情
- [ ] 后台用户管理

---

## 注意事项

1. **Token验证**：所有需要登录的接口都要验证Header中的Authorization
2. **统一返回格式**：所有接口返回格式要一致，用code表示状态（0成功，其他失败）
3. **错误处理**：返回具体的错误信息，方便前端提示
4. **订单号生成**：建议用 "WX" + 时间戳
5. **时间格式**：建议用时间戳（毫秒）
